apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-postgres-pvc
  namespace: firefly
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: storage-db
  volumeName: firefly-postgres-pv
