apiVersion: v1
kind: Service
metadata:
  name: guacamole-db
  namespace: homelab
  labels:
    app: guacamole-db
spec:
  type: ClusterIP
  selector:
    app: guacamole-db
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
