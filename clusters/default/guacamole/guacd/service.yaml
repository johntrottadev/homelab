apiVersion: v1
kind: Service
metadata:
  name: guacd
  namespace: guacamole
  labels:
    app: guacd
spec:
  type: ClusterIP
  selector:
    app: guacd
  ports:
    - port: 4822
      targetPort: 4822
      protocol: TCP
