apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: guac
  namespace: homelab
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`guac.${BASE_DOMAIN}`)
      kind: Rule
      services:
        - name: guacamole-service
          port: 8080
