apiVersion: v1
kind: Service
metadata:
  name: pihole-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: pihole-exporter
spec:
  ports:
  - name: metrics
    port: 9617
    protocol: TCP
    targetPort: metrics
  selector:
    app.kubernetes.io/name: pihole-exporter
  type: ClusterIP

