apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: netalert
  namespace: netalert
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`netalert.__BASE-DOMAIN__`)
      kind: Rule
      services:
        - name: netalert
          port: 20211
