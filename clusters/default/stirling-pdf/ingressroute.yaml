apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: stirling-pdf
  namespace: stirling-pdf
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`stirling.${BASE_DOMAIN}`)
      kind: Rule
      services:
        - name: stirling-pdf
          port: 80
